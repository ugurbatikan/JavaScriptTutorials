/* // RETURN
Builts-in methods return values when we call them
we can store those values */
 //NO RETURN 
 //Our functions print values out, but do NOT  return anything
 
 /* function add (x, y){
     console.log(x + y);
 }
 add(2,3); // 2 + 3 = 5
 const sum = add(10, 6); // 16
 sum; // Undefined */

 // RETURN
  
 function add(x, y){
    return x + y;
 }
 const sum = add (10, 6);
 sum; // 16
 const answer = add (100,150);
 answer;